diff -uNr pine4.30.orig/build pine4.30/build
--- pine4.30.orig/build	Mon Oct 30 15:41:31 2000
+++ pine4.30/build	Mon Oct 30 15:43:23 2000
@@ -95,7 +95,7 @@
  pine   The Pine mailer. Once compiled this should work just fine on
         your system with no other files than this binary, and no
         modifications to your system. Optionally you may create two
-        configuration files, /usr/local/lib/pine.conf and 
+        configuration files, /etc/pine.conf and 
         /usr/local/lib/pine.info. See the documentation for details.
  
  pico   The standalone editor similar to the Pine message composer.
diff -uNr pine4.30.orig/doc/pine.1 pine4.30/doc/pine.1
--- pine4.30.orig/doc/pine.1	Wed Oct 25 16:31:01 2000
+++ pine4.30/doc/pine.1	Mon Oct 30 15:43:23 2000
@@ -284,7 +284,7 @@
 .if n .ta 2.8i
 .if t .ta 2.1i
 
-/usr/spool/mail/xxxx	Default folder for incoming mail.
+/var/spool/mail/xxxx	Default folder for incoming mail.
 .br
 ~/mail	Default directory for mail folders.
 .br
@@ -308,13 +308,13 @@
 .br
 /etc/mime.types	System-wide file ext. to MIME type mapping
 .br
-/usr/local/lib/pine.info	Local pointer to system administrator.
+/usr/lib/pine.info	Local pointer to system administrator.
 .br
-/usr/local/lib/pine.conf	System-wide configuration file.
+/etc/pine.conf	System-wide configuration file.
 .br
-/usr/local/lib/pine.conf.fixed	 Non-overridable configuration file.
+/etc/pine.conf.fixed	 Non-overridable configuration file.
 .br
-/tmp/.\\usr\\spool\\mail\\xxxx	Per-folder mailbox lock files.
+/tmp/.\\var\\spool\\mail\\xxxx	Per-folder mailbox lock files.
 .br
 ~/.pine-interrupted-mail	Message which was interrupted.
 .br
diff -uNr pine4.30.orig/doc/tech-notes/background.html pine4.30/doc/tech-notes/background.html
--- pine4.30.orig/doc/tech-notes/background.html	Tue Dec 15 16:10:07 1998
+++ pine4.30/doc/tech-notes/background.html	Mon Oct 30 15:43:23 2000
@@ -174,7 +174,7 @@
 <OL>
 
 <LI><EM>sendmail-path</EM> in
-<CODE>/usr/local/lib/pine.conf.fixed</CODE>
+<CODE>/etc/pine.conf.fixed</CODE>
 
 <LI><EM>smtp-server</EM> in <CODE>/usr/local/pine.conf.fixed</CODE>
 
@@ -186,7 +186,7 @@
 
 <LI><EM>smtp-server</EM> in the user's <CODE>.pinerc</CODE> file. 
 
-<LI><EM>sendmail-path</EM> in <CODE>/usr/local/lib/pine.conf</CODE>
+<LI><EM>sendmail-path</EM> in <CODE>/etc/pine.conf</CODE>
 
 <LI><EM>smtp-server</EM> in <CODE>/usr/local/pine.conf</CODE>
 
diff -uNr pine4.30.orig/doc/tech-notes/cmd-line.html pine4.30/doc/tech-notes/cmd-line.html
--- pine4.30.orig/doc/tech-notes/cmd-line.html	Thu Oct 19 13:07:47 2000
+++ pine4.30/doc/tech-notes/cmd-line.html	Mon Oct 30 15:43:23 2000
@@ -81,7 +81,7 @@
 file, execute
 
 <PRE><CODE>
-		pine -conf > /usr/local/lib/pine.conf
+		pine -conf > /etc/pine.conf
 </CODE></PRE>
 <P>
 
@@ -89,7 +89,7 @@
 system configuration file, execute
 
 <PRE><CODE>
-		pine -P old-pine.conf -conf > /usr/local/lib/pine.conf
+		pine -P old-pine.conf -conf > /etc/pine.conf
 </CODE></PRE>
 <P>
 A system configuration file is not required.
@@ -243,7 +243,7 @@
 <DT> -P <EM>pinerc</EM>
 
 <DD> Uses the named file as the system wide configuration file instead of
-<EM>/usr/local/lib/pine.conf</EM> on UNIX, or nothing on <EM>PC-Pine</EM>.
+<EM>/etc/pine.conf</EM> on UNIX, or nothing on <EM>PC-Pine</EM>.
 Pinerc may be either a local file or a remote configuration folder.
 <P>
 
diff -uNr pine4.30.orig/doc/tech-notes/config-notes.html pine4.30/doc/tech-notes/config-notes.html
--- pine4.30.orig/doc/tech-notes/config-notes.html	Thu Oct 19 13:07:47 2000
+++ pine4.30/doc/tech-notes/config-notes.html	Mon Oct 30 15:43:23 2000
@@ -442,10 +442,10 @@
 </OL>
 <P>
 The fixed configuration file is normally
-<CODE>/usr/local/lib/pine.conf.fixed</CODE>.
+<CODE>/etc/pine.conf.fixed</CODE>.
 <P>
 The system-wide configuration file is normally
-<CODE>/usr/local/lib/pine.conf</CODE> for Unix <EM>Pine</EM> and is normally not
+<CODE>/etc/pine.conf</CODE> for Unix <EM>Pine</EM> and is normally not
 set for <EM>PC-Pine</EM>.
 For <EM>PC-Pine</EM>, if the environment variable <EM>$PINECONF</EM> is set, that
 is used for the system-wide configuration.
@@ -647,8 +647,8 @@
 
 <LI> An entry in <CODE>/etc/inetd.conf</CODE> for the alternate service.
 
-<LI> An entry in <CODE>/usr/local/lib/pine.conf</CODE>,
-<CODE>/usr/local/lib/pine.conf.fixed</CODE> or <CODE>~/.pinerc</CODE>. 
+<LI> An entry in <CODE>/etc/pine.conf</CODE>,
+<CODE>/etc/pine.conf.fixed</CODE> or <CODE>~/.pinerc</CODE>. 
 
 </UL>
 
diff -uNr pine4.30.orig/doc/tech-notes/config.html pine4.30/doc/tech-notes/config.html
--- pine4.30.orig/doc/tech-notes/config.html	Tue Oct 24 20:47:33 2000
+++ pine4.30/doc/tech-notes/config.html	Mon Oct 30 15:43:23 2000
@@ -7,8 +7,8 @@
 configuration. In most cases, the compiled-in preferences will suit users
 and administrators just fine. When running <EM>Pine</EM> on a UNIX system, the
 default built-in configuration can be changed by setting variables in the
-system configuration files, <CODE>/usr/local/lib/pine.conf</CODE>
-or <CODE>/usr/local/lib/pine.conf.fixed</CODE>.
+system configuration files, <CODE>/etc/pine.conf</CODE>
+or <CODE>/etc/pine.conf.fixed</CODE>.
 (Actually, these files are whatever the definitions for
 <CODE>SYSTEM_PINERC</CODE> and <CODE>SYSTEM_PINERC_FIXED</CODE>
 in pine/osdep/os-xxx.h are set to.)
@@ -1935,7 +1935,7 @@
 alternate spell checker.
 If your Unix system has <EM>ispell</EM> it is probably reasonable to make
 it the default speller by configuring it as the default in the
-system configuration file, <CODE>/usr/local/lib/pine.conf</CODE>.
+system configuration file, <CODE>/etc/pine.conf</CODE>.
 
 <P>
 If this option is not set, then the system's <EM>spell</EM> command is used.
diff -uNr pine4.30.orig/doc/tech-notes/installation.html pine4.30/doc/tech-notes/installation.html
--- pine4.30.orig/doc/tech-notes/installation.html	Mon Oct 16 15:24:01 2000
+++ pine4.30/doc/tech-notes/installation.html	Mon Oct 30 15:43:23 2000
@@ -209,7 +209,7 @@
 <DD> The name of the file which holds <EM>Pine</EM>
 configuration information for
 all users on the system.  Default on UNIX systems is
-<CODE>/usr/local/lib/pine.conf</CODE>.
+<CODE>/etc/pine.conf</CODE>.
 
 <DT> SYSTEM_PINERC_FIXED
 
@@ -217,7 +217,7 @@
 SYSTEM_PINERC, but only for variables that the administrator wants to keep
 fixed.  That is, users are not allowed to change variables that are
 specified in the FIXED file.  Default on UNIX systems is
-<CODE>/usr/local/lib/pine.conf.fixed</CODE>.
+<CODE>/etc/pine.conf.fixed</CODE>.
 
 </DL> <P>
 
@@ -416,8 +416,8 @@
 
 There are, however, three optional auxiliary files: 
 <CODE>/usr/local/lib/pine.info</CODE>,
-<CODE>/usr/local/lib/pine.conf</CODE>, and
-<CODE>/usr/local/lib/pine.conf.fixed</CODE>.  The file
+<CODE>/etc/pine.conf</CODE>, and
+<CODE>/etc/pine.conf.fixed</CODE>.  The file
 <CODE>pine.info</CODE> contains text on how to get further help on the
 local system.  It is part of the help text for the
 main menu and should probably refer to the local help desk or the system
@@ -581,11 +581,11 @@
 
 <DL COMPACT>
 
-<DT> /usr/local/lib/pine.conf
+<DT> /etc/pine.conf
 
 <DD> Pine's global configuration file. 
 
-<DT> /usr/local/lib/pine.conf.fixed
+<DT> /etc/pine.conf.fixed
 
 <DD> Non-overridable global configuration file. 
 
diff -uNr pine4.30.orig/doc/tech-notes/low-level.html pine4.30/doc/tech-notes/low-level.html
--- pine4.30.orig/doc/tech-notes/low-level.html	Fri May 12 15:21:32 2000
+++ pine4.30/doc/tech-notes/low-level.html	Mon Oct 30 15:43:23 2000
@@ -1086,7 +1086,7 @@
 
 The second selection is the standard UNIX print command.  The default is
 <EM>lpr</EM>, but it can be changed on a system basis to anything so
-desired in <CODE>/usr/local/lib/pine.conf</CODE>. <P>
+desired in <CODE>/etc/pine.conf</CODE>. <P>
 
 The third selection is
 the user's personal choice for a UNIX print command.  The text to be
diff -uNr pine4.30.orig/doc/tech-notes/porting.html pine4.30/doc/tech-notes/porting.html
--- pine4.30.orig/doc/tech-notes/porting.html	Wed Sep 16 19:09:04 1998
+++ pine4.30/doc/tech-notes/porting.html	Mon Oct 30 15:43:23 2000
@@ -307,11 +307,11 @@
 <DT>
  ___
 <DD>
-Reading of <CODE>/usr/local/lib/pine.conf</CODE> 
+Reading of <CODE>/etc/pine.conf</CODE> 
 <DT>
  ___
 <DD>
-Fixing variables and features in <CODE>/usr/local/lib/pine.conf.fixed</CODE> 
+Fixing variables and features in <CODE>/etc/pine.conf.fixed</CODE> 
 <DT>
  ___
 <DD>
diff -uNr pine4.30.orig/doc/tech-notes.txt pine4.30/doc/tech-notes.txt
--- pine4.30.orig/doc/tech-notes.txt	Tue Oct 24 20:48:46 2000
+++ pine4.30/doc/tech-notes.txt	Mon Oct 30 15:43:23 2000
@@ -327,14 +327,14 @@
           sendmail-path, smtp-server, and compile-time options. The first
           MTA specified in the following list is used:
           
-         1. _sendmail-path_ in /usr/local/lib/pine.conf.fixed
-         2. _smtp-server_ in /usr/local/pine.conf.fixed
+         1. _sendmail-path_ in /etc/pine.conf.fixed
+         2. _smtp-server_ in /etc/pine.conf.fixed
          3. _sendmail-path_ specified on the command line.
          4. _smtp-server_ specified on the command line.
          5. _sendmail-path_ in the user's .pinerc file.
          6. _smtp-server_ in the user's .pinerc file.
-         7. _sendmail-path_ in /usr/local/lib/pine.conf
-         8. _smtp-server_ in /usr/local/pine.conf
+         7. _sendmail-path_ in /etc/pine.conf
+         8. _smtp-server_ in /etc/pine.conf
          9. DF_SENDMAIL_PATH defined at compile time.
         10. SENDMAIL and SENDMAILFLAGS defined at compile time.
             
@@ -657,14 +657,14 @@
    SYSTEM_PINERC
           The name of the file which holds _Pine_ configuration
           information for all users on the system. Default on UNIX
-          systems is /usr/local/lib/pine.conf.
+          systems is /etc/pine.conf.
    SYSTEM_PINERC_FIXED
           The name of the file which holds the same type of information
           as for SYSTEM_PINERC, but only for variables that the
           administrator wants to keep fixed. That is, users are not
           allowed to change variables that are specified in the FIXED
           file. Default on UNIX systems is
-          /usr/local/lib/pine.conf.fixed.
+          /etc/pine.conf.fixed.
           
    There are a couple of more obscure options which are in the source
    code because a few people have asked for them or because we changed
@@ -800,8 +800,8 @@
    auxiliary files.
    
    There are, however, three optional auxiliary files:
-   /usr/local/lib/pine.info, /usr/local/lib/pine.conf, and
-   /usr/local/lib/pine.conf.fixed. The file pine.info contains text on
+   /usr/lib/pine.info, /etc/pine.conf, and
+   /etc/pine.conf.fixed. The file pine.info contains text on
    how to get further help on the local system. It is part of the help
    text for the main menu and should probably refer to the local help
    desk or the system administrator. If this file doesn't exist a generic
@@ -922,11 +922,11 @@
    This section lists the various files which _Pine_ uses which are not
    email folders. All of these are the default names of files, they may
    vary based on _Pine_'s configuration.
-   /usr/local/lib/pine.conf
+   /etc/pine.conf
           Pine's global configuration file.
-   /usr/local/lib/pine.conf.fixed
+   /etc/pine.conf.fixed
           Non-overridable global configuration file.
-   /usr/local/lib/pine.info
+   /usr/lib/pine.info
           Local pointer to system administrator.
    ~/.pinerc
           Personal configuration file for each user.
@@ -1139,10 +1139,10 @@
           Configuration: Prints a sample system configuration file to the
           screen or standard output. To generate an initial system
           configuration file, execute
-                pine -conf > /usr/local/lib/pine.conf
+                pine -conf > /etc/pine.conf
           To generate a system configuration file using settings from an
           old system configuration file, execute
-                pine -P old-pine.conf -conf > /usr/local/lib/pine.conf
+                pine -P old-pine.conf -conf > /etc/pine.conf
           A system configuration file is not required.
    -copy_abook _<local_abook_file> <remote_abook_folder>_
           Copy an address book file to a remote address book folder. If
@@ -1242,7 +1242,7 @@
           configuration folder.
    -P _pinerc_
           Uses the named file as the system wide configuration file
-          instead of _/usr/local/lib/pine.conf_ on UNIX, or nothing on
+          instead of _/etc/pine.conf_ on UNIX, or nothing on
           _PC-Pine_. Pinerc may be either a local file or a remote
           configuration folder.
    -pinerc _file_
@@ -1424,8 +1424,8 @@
    configuration. In most cases, the compiled-in preferences will suit
    users and administrators just fine. When running _Pine_ on a UNIX
    system, the default built-in configuration can be changed by setting
-   variables in the system configuration files, /usr/local/lib/pine.conf
-   or /usr/local/lib/pine.conf.fixed. (Actually, these files are whatever
+   variables in the system configuration files, /etc/pine.conf
+   or /etc/pine.conf.fixed. (Actually, these files are whatever
    the definitions for SYSTEM_PINERC and SYSTEM_PINERC_FIXED in
    pine/osdep/os-xxx.h are set to.) The location of the pine.conf file
    can be changed with the -P command line argument. Both _Pine_ and
@@ -2827,7 +2827,7 @@
           spell checker. If your Unix system has _ispell_ it is probably
           reasonable to make it the default speller by configuring it as
           the default in the system configuration file,
-          /usr/local/lib/pine.conf.
+          /etc/pine.conf.
           If this option is not set, then the system's _spell_ command is
           used. The spell command does not work the same as the alternate
           speller. It produces a list of misspelled words on its standard
@@ -2849,7 +2849,7 @@
           set to zero ssh connections will be completely disabled.
    _ssh-path_
           Sets the name of the command used to open a UNIX secure shell
-          connection. The default is typically /usr/local/bin/ssh.
+          connection. The default is typically /usr/bin/ssh.
    _standard-printer_
           System-wide configuration file only. Specifies a list of
           commands for category 2 of the _Setup/Printer_ screen, the
@@ -2923,12 +2923,12 @@
           relative to your home directory).
           Now for an example:
           
-     url-viewers=_TEST("test -n '${DISPLAY}'")_ /usr/local/bin/netscape,
-     /usr/local/bin/lynx, C:\BIN\NETSCAPE.BAT
+     url-viewers=_TEST("test -n '${DISPLAY}'")_ /usr/bin/netscape,
+     /usr/bin/lynx, C:\BIN\NETSCAPE.BAT
           This example shows that for the first browser in the list to be
           used the environment variable DISPLAY must be defined. If it
-          is, then the file /usr/local/bin/netscape must exist. If either
-          condition is not met, then the file /usr/local/bin/lynx must
+          is, then the file /usr/bin/netscape must exist. If either
+          condition is not met, then the file /usr/bin/lynx must
           exist. If it doesn't, then the final path and file must exist.
           Note that the last entry is a DOS/Windows path. This is one way
           to support _Pine_ running on more than one architecture with
@@ -6263,10 +6263,10 @@
     5. the system-wide _fixed_ configuration file (Unix _Pine_ only)
        
    The fixed configuration file is normally
-   /usr/local/lib/pine.conf.fixed.
+   /etc/pine.conf.fixed.
    
    The system-wide configuration file is normally
-   /usr/local/lib/pine.conf for Unix _Pine_ and is normally not set for
+   /etc/pine.conf for Unix _Pine_ and is normally not set for
    _PC-Pine_. For _PC-Pine_, if the environment variable _$PINECONF_ is
    set, that is used for the system-wide configuration. This location can
    be set or changed on the command line with the -P flag. The
@@ -6420,8 +6420,8 @@
      * A program that implements the SMTP or ESMTP protocol via stdio.
      * An entry in /etc/services for the alternate service.
      * An entry in /etc/inetd.conf for the alternate service.
-     * An entry in /usr/local/lib/pine.conf,
-       /usr/local/lib/pine.conf.fixed or ~/.pinerc.
+     * An entry in /etc/pine.conf,
+       /etc/pine.conf.fixed or ~/.pinerc.
      _________________________________________________________________
    
 MIME.Types file
@@ -7610,7 +7610,7 @@
    
    The second selection is the standard UNIX print command. The default
    is _lpr_, but it can be changed on a system basis to anything so
-   desired in /usr/local/lib/pine.conf.
+   desired in /etc/pine.conf.
    
    The third selection is the user's personal choice for a UNIX print
    command. The text to be printed is piped into the command. _Enscript_
@@ -8064,9 +8064,9 @@
           Error messages for incorrect terminal types (try "foo" and
           "vt52")
    ___
-          Reading of /usr/local/lib/pine.conf
+          Reading of /etc/pine.conf
    ___
-          Fixing variables and features in /usr/local/lib/pine.conf.fixed
+          Fixing variables and features in /etc/pine.conf.fixed
    ___
           Flag command (check message status changed in mail folder)
    ___
diff -uNr pine4.30.orig/imap/src/osdep/unix/Makefile pine4.30/imap/src/osdep/unix/Makefile
--- pine4.30.orig/imap/src/osdep/unix/Makefile	Mon Oct 30 15:41:31 2000
+++ pine4.30/imap/src/osdep/unix/Makefile	Mon Oct 30 15:43:24 2000
@@ -382,7 +382,7 @@
 	 SPOOLDIR=/var/spool \
 	 ACTIVEFILE=/var/lib/news/active \
 	 RSHPATH=/usr/bin/rsh \
-	 BASECFLAGS="-g -fno-omit-frame-pointer -O6" \
+	 BASECFLAGS="-fno-omit-frame-pointer -O6 $(RPM_OPT_FLAGS)" \
 	 BASELDFLAGS="-lpam -ldl"
 
 lnx:	# Linux non-shadow passwords
@@ -395,7 +395,7 @@
 	 SPOOLDIR=/var/spool \
 	 ACTIVEFILE=/var/lib/news/active \
 	 RSHPATH=/usr/bin/rsh \
-	 BASECFLAGS="-g -O"
+	 BASECFLAGS="-g -O $(RPM_OPT_FLAGS)"
 
 lyn:	# LynxOS
 	$(BUILD) `$(CAT) SPECIALS` OS=$@ \
diff -uNr pine4.30.orig/pine/init.c pine4.30/pine/init.c
--- pine4.30.orig/pine/init.c	Mon Oct 30 15:41:31 2000
+++ pine4.30/pine/init.c	Mon Oct 30 15:43:26 2000
@@ -368,7 +368,7 @@
 
 /*----------------------------------------------------------------------
 These are the variables that control a number of pine functions.  They
-come out of the .pinerc and the /usr/local/lib/pine.conf files.  Some can
+come out of the .pinerc and the /etc/pine.conf files.  Some can
 be set by the user while in Pine.  Eventually all the local ones should
 be so and maybe the global ones too.
 
diff -uNr pine4.30.orig/pine/osdep/os-a32.h pine4.30/pine/osdep/os-a32.h
--- pine4.30.orig/pine/osdep/os-a32.h	Wed Oct  7 17:51:35 1998
+++ pine4.30/pine/osdep/os-a32.h	Mon Oct 30 15:43:25 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-a41.h pine4.30/pine/osdep/os-a41.h
--- pine4.30.orig/pine/osdep/os-a41.h	Wed Oct  7 17:51:36 1998
+++ pine4.30/pine/osdep/os-a41.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-aix.h pine4.30/pine/osdep/os-aix.h
--- pine4.30.orig/pine/osdep/os-aix.h	Wed Oct  7 17:51:36 1998
+++ pine4.30/pine/osdep/os-aix.h	Mon Oct 30 15:43:25 2000
@@ -76,7 +76,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -162,8 +162,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-aux.h pine4.30/pine/osdep/os-aux.h
--- pine4.30.orig/pine/osdep/os-aux.h	Wed Oct  7 17:51:38 1998
+++ pine4.30/pine/osdep/os-aux.h	Mon Oct 30 15:43:26 2000
@@ -75,7 +75,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -161,8 +161,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-bs2.h pine4.30/pine/osdep/os-bs2.h
--- pine4.30.orig/pine/osdep/os-bs2.h	Wed Oct  7 17:51:39 1998
+++ pine4.30/pine/osdep/os-bs2.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
diff -uNr pine4.30.orig/pine/osdep/os-bsd.h pine4.30/pine/osdep/os-bsd.h
--- pine4.30.orig/pine/osdep/os-bsd.h	Wed Oct  7 17:51:39 1998
+++ pine4.30/pine/osdep/os-bsd.h	Mon Oct 30 15:43:25 2000
@@ -89,7 +89,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -175,8 +175,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-bsf.h pine4.30/pine/osdep/os-bsf.h
--- pine4.30.orig/pine/osdep/os-bsf.h	Wed Oct  7 17:51:40 1998
+++ pine4.30/pine/osdep/os-bsf.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-bsi.h pine4.30/pine/osdep/os-bsi.h
--- pine4.30.orig/pine/osdep/os-bsi.h	Wed Oct  7 17:51:41 1998
+++ pine4.30/pine/osdep/os-bsi.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-bso.h pine4.30/pine/osdep/os-bso.h
--- pine4.30.orig/pine/osdep/os-bso.h	Fri Jan 22 13:40:01 1999
+++ pine4.30/pine/osdep/os-bso.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
diff -uNr pine4.30.orig/pine/osdep/os-cvx.h pine4.30/pine/osdep/os-cvx.h
--- pine4.30.orig/pine/osdep/os-cvx.h	Wed Oct  7 17:51:42 1998
+++ pine4.30/pine/osdep/os-cvx.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-dos.h pine4.30/pine/osdep/os-dos.h
--- pine4.30.orig/pine/osdep/os-dos.h	Mon Nov 23 20:56:24 1998
+++ pine4.30/pine/osdep/os-dos.h	Mon Oct 30 15:43:26 2000
@@ -75,7 +75,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
diff -uNr pine4.30.orig/pine/osdep/os-dpx.h pine4.30/pine/osdep/os-dpx.h
--- pine4.30.orig/pine/osdep/os-dpx.h	Wed Oct  7 17:51:43 1998
+++ pine4.30/pine/osdep/os-dpx.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-dyn.h pine4.30/pine/osdep/os-dyn.h
--- pine4.30.orig/pine/osdep/os-dyn.h	Wed Oct  7 17:51:44 1998
+++ pine4.30/pine/osdep/os-dyn.h	Mon Oct 30 15:43:25 2000
@@ -75,7 +75,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -161,8 +161,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-gen.h pine4.30/pine/osdep/os-gen.h
--- pine4.30.orig/pine/osdep/os-gen.h	Wed Oct  7 17:51:45 1998
+++ pine4.30/pine/osdep/os-gen.h	Mon Oct 30 15:43:25 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-hpp.h pine4.30/pine/osdep/os-hpp.h
--- pine4.30.orig/pine/osdep/os-hpp.h	Wed Oct  7 17:51:45 1998
+++ pine4.30/pine/osdep/os-hpp.h	Mon Oct 30 15:43:25 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-hpx.h pine4.30/pine/osdep/os-hpx.h
--- pine4.30.orig/pine/osdep/os-hpx.h	Wed Oct  7 17:51:46 1998
+++ pine4.30/pine/osdep/os-hpx.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-isc.h pine4.30/pine/osdep/os-isc.h
--- pine4.30.orig/pine/osdep/os-isc.h	Wed Oct  7 17:51:46 1998
+++ pine4.30/pine/osdep/os-isc.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-lnx.h pine4.30/pine/osdep/os-lnx.h
--- pine4.30.orig/pine/osdep/os-lnx.h	Mon Oct 30 15:41:31 2000
+++ pine4.30/pine/osdep/os-lnx.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-lyn.h pine4.30/pine/osdep/os-lyn.h
--- pine4.30.orig/pine/osdep/os-lyn.h	Wed Oct  7 17:51:47 1998
+++ pine4.30/pine/osdep/os-lyn.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-mnt.h pine4.30/pine/osdep/os-mnt.h
--- pine4.30.orig/pine/osdep/os-mnt.h	Wed Oct  7 17:51:48 1998
+++ pine4.30/pine/osdep/os-mnt.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-neb.h pine4.30/pine/osdep/os-neb.h
--- pine4.30.orig/pine/osdep/os-neb.h	Thu Jan  7 14:47:35 1999
+++ pine4.30/pine/osdep/os-neb.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-nxt.h pine4.30/pine/osdep/os-nxt.h
--- pine4.30.orig/pine/osdep/os-nxt.h	Wed Oct  7 17:51:49 1998
+++ pine4.30/pine/osdep/os-nxt.h	Mon Oct 30 15:43:25 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-os2.h pine4.30/pine/osdep/os-os2.h
--- pine4.30.orig/pine/osdep/os-os2.h	Mon Nov 23 20:56:33 1998
+++ pine4.30/pine/osdep/os-os2.h	Mon Oct 30 15:43:26 2000
@@ -75,7 +75,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
diff -uNr pine4.30.orig/pine/osdep/os-osf.h pine4.30/pine/osdep/os-osf.h
--- pine4.30.orig/pine/osdep/os-osf.h	Wed Oct  7 17:51:50 1998
+++ pine4.30/pine/osdep/os-osf.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-osx.h pine4.30/pine/osdep/os-osx.h
--- pine4.30.orig/pine/osdep/os-osx.h	Wed Sep 20 19:59:29 2000
+++ pine4.30/pine/osdep/os-osx.h	Mon Oct 30 15:43:26 2000
@@ -89,7 +89,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -175,8 +175,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-pt1.h pine4.30/pine/osdep/os-pt1.h
--- pine4.30.orig/pine/osdep/os-pt1.h	Wed Oct  7 17:51:51 1998
+++ pine4.30/pine/osdep/os-pt1.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-ptx.h pine4.30/pine/osdep/os-ptx.h
--- pine4.30.orig/pine/osdep/os-ptx.h	Wed Oct  7 17:51:51 1998
+++ pine4.30/pine/osdep/os-ptx.h	Mon Oct 30 15:43:25 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-s40.h pine4.30/pine/osdep/os-s40.h
--- pine4.30.orig/pine/osdep/os-s40.h	Wed Oct  7 17:51:52 1998
+++ pine4.30/pine/osdep/os-s40.h	Mon Oct 30 15:43:25 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-sc5.h pine4.30/pine/osdep/os-sc5.h
--- pine4.30.orig/pine/osdep/os-sc5.h	Wed Oct  7 17:51:52 1998
+++ pine4.30/pine/osdep/os-sc5.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 /*----------------------------------------------------------------------
diff -uNr pine4.30.orig/pine/osdep/os-sco.h pine4.30/pine/osdep/os-sco.h
--- pine4.30.orig/pine/osdep/os-sco.h	Wed Oct  7 17:51:53 1998
+++ pine4.30/pine/osdep/os-sco.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 /*----------------------------------------------------------------------
diff -uNr pine4.30.orig/pine/osdep/os-sgi.h pine4.30/pine/osdep/os-sgi.h
--- pine4.30.orig/pine/osdep/os-sgi.h	Wed Oct  7 17:51:53 1998
+++ pine4.30/pine/osdep/os-sgi.h	Mon Oct 30 15:43:25 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-sol.h pine4.30/pine/osdep/os-sol.h
--- pine4.30.orig/pine/osdep/os-sol.h	Wed Oct  7 17:51:54 1998
+++ pine4.30/pine/osdep/os-sol.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-sun.h pine4.30/pine/osdep/os-sun.h
--- pine4.30.orig/pine/osdep/os-sun.h	Wed Oct  7 17:51:54 1998
+++ pine4.30/pine/osdep/os-sun.h	Mon Oct 30 15:43:25 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-sv4.h pine4.30/pine/osdep/os-sv4.h
--- pine4.30.orig/pine/osdep/os-sv4.h	Wed Oct  7 17:51:55 1998
+++ pine4.30/pine/osdep/os-sv4.h	Mon Oct 30 15:43:25 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/osdep/os-ult.h pine4.30/pine/osdep/os-ult.h
--- pine4.30.orig/pine/osdep/os-ult.h	Wed Oct  7 17:51:56 1998
+++ pine4.30/pine/osdep/os-ult.h	Mon Oct 30 15:43:26 2000
@@ -74,7 +74,7 @@
    default-composer-hdrs or customized-hdrs to get at it. Instead of
    defining NEVER_ALLOW_CHANGING_FROM, an easier way of preventing From
    changing is to put the feature "no-allow-changing-from" in the
-   /usr/local/lib/pine.conf.fixed file.
+   /etc/pine.conf.fixed file.
  ----*/
 /* #define NEVER_ALLOW_CHANGING_FROM */
 
@@ -160,8 +160,8 @@
 
 
 /*----- System-wide config file ----------------------------------------*/
-#define SYSTEM_PINERC             "/usr/local/lib/pine.conf"
-#define SYSTEM_PINERC_FIXED       "/usr/local/lib/pine.conf.fixed"
+#define SYSTEM_PINERC             "/etc/pine.conf"
+#define SYSTEM_PINERC_FIXED       "/etc/pine.conf.fixed"
 
 
 
diff -uNr pine4.30.orig/pine/pine-use.c pine4.30/pine/pine-use.c
--- pine4.30.orig/pine/pine-use.c	Wed Feb 16 18:53:08 2000
+++ pine4.30/pine/pine-use.c	Mon Oct 30 15:43:26 2000
@@ -45,8 +45,8 @@
 #include <sys/stat.h>
 
 #ifndef MAILSPOOLPCTS
-#define MAILSPOOLPCTS "/usr/spool/mail/%s"
-/* #define MAILSPOOLPCTS "/usr/mail/%s" */
+#define MAILSPOOLPCTS "/var/spool/mail/%s"
+/* #define MAILSPOOLPCTS "/var/mail/%s" */
 #endif
 
 #define DAYSEC (60*60*24)
diff -uNr pine4.30.orig/pine/pine.hlp pine4.30/pine/pine.hlp
--- pine4.30.orig/pine/pine.hlp	Wed Oct 25 12:17:47 2000
+++ pine4.30/pine/pine.hlp	Mon Oct 30 15:43:27 2000
@@ -550,9 +550,9 @@
  executable     &lt;Unix search path&gt;/pine
  persnl cfg     ~/.pinerc
  except cfg     ~/.pinercex
- global cfg     /usr/local/lib/pine.conf
- fixed cfg      /usr/local/lib/pine.conf.fixed
- local help     /usr/local/lib/pine.info
+ global cfg     /etc/pine.conf
+ fixed cfg      /etc/pine.conf.fixed
+ local help     /usr/lib/pine.info
 
  interrupted    ~/.pine-interrupted-mail
  debug          ~/.pine-debugN
@@ -565,12 +565,12 @@
  sentmail#      ~/mail/sent-mail
  mailcap#       ~/.mailcap + /etc/mailcap
                            + /usr/etc/mailcap + /usr/local/etc/mailcap
- mimetypes#     ~/.mime.types + /etc/mime.types + /usr/local/lib/mime.types
+ mimetypes#     ~/.mime.types + /etc/mime.types + /usr/lib/mime.types
 
- news-spool	varies across Unix flavors, e.g. /usr/spool/news or /var/news
+ news-spool	varies across Unix flavors, e.g. /var/spool/news or /var/news
  active-news	varies across Unix flavors, e.g. /usr/lib/news/active
- lock files     /tmp/.&#92;usr&#92;spool&#92;mail&#92;xxxx
- inbox          /usr/spool/mail/xxxx
+ lock files     /tmp/.&#92;var&#92;spool&#92;mail&#92;xxxx
+ inbox          /var/spool/mail/xxxx
  password       /etc/passwd
 
 Unix Pine and PC-Pine:
@@ -1022,9 +1022,9 @@
 this link now to send a message to it:<BR>
 <A HREF="X-Pine-Gripe:_LOCAL_ADDRESS_?local"><!--#echo var="_LOCAL_FULLNAME_"--></A><P>
 <!--chtml endif-->
-<!--chtml if [ -r /usr/local/lib/pine.info ]-->
+<!--chtml if [ -r /usr/lib/pine.info ]-->
 <HR WIDTH="75%">Local Support Contacts:<P>
-<!--#include file="/usr/local/lib/pine.info"-->
+<!--#include file="/usr/lib/pine.info"-->
 <HR WIDTH="75%">
 <!--chtml endif-->
 <P> 
@@ -1112,7 +1112,7 @@
 <OL>
   <LI> <A HREF="#intro">Introduction</A>
   <LI> <A HREF="#pine_help">Pine Help</A>
-<!--chtml if [ -r /usr/local/lib/pine.info ]-->
+<!--chtml if [ -r /usr/lib/pine.info ]-->
   <LI> <A HREF="#local_support">Local Support Contacts</A>
 <!--chtml endif-->
   <LI> <A HREF="#giving_cmds">Giving Commands in Pine</A>
@@ -1221,9 +1221,9 @@
 at your site.)
 
 
-<!--chtml if [ -r /usr/local/lib/pine.info ]-->
+<!--chtml if [ -r /usr/lib/pine.info ]-->
 <H2><A NAME="local_support">Local Support Contacts</A></H2>
-<!--#include file="/usr/local/lib/pine.info"-->
+<!--#include file="/usr/lib/pine.info"-->
 <!--chtml endif-->
 
 <H2><A NAME="giving_cmds">Giving Commands in Pine</A></H2>
@@ -1877,7 +1877,7 @@
 file, execute
 
 <PRE><CODE>
-		pine -conf > /usr/local/lib/pine.conf
+		pine -conf > /etc/pine.conf
 </CODE></PRE>
 <P>
 
@@ -1885,7 +1885,7 @@
 system configuration file, execute
 
 <PRE><CODE>
-		pine -P old-pine.conf -conf > /usr/local/lib/pine.conf
+		pine -P old-pine.conf -conf > /etc/pine.conf
 </CODE></PRE>
 <P>
 A system configuration file is not required.
@@ -2036,7 +2036,7 @@
 <DT> -P <EM>pinerc</EM>
 
 <DD> Uses the named file as the system wide configuration file instead of
-<EM>/usr/local/lib/pine.conf</EM> on UNIX, or nothing on <EM>PC-Pine</EM>.
+<EM>/etc/pine.conf</EM> on UNIX, or nothing on <EM>PC-Pine</EM>.
 Pinerc may be either a local file or a remote configuration folder.
 <P>
 
@@ -2737,10 +2737,10 @@
 </OL>
 <P>
 The fixed configuration file is normally
-<CODE>/usr/local/lib/pine.conf.fixed</CODE>.
+<CODE>/etc/pine.conf.fixed</CODE>.
 <P>
 The system-wide configuration file is normally
-<CODE>/usr/local/lib/pine.conf</CODE> for Unix Pine and is normally not
+<CODE>/etc/pine.conf</CODE> for Unix Pine and is normally not
 set for PC-Pine.
 For PC-Pine, if the environment variable <EM>$PINECONF</EM> is set, that
 is used for the system-wide configuration.
@@ -15573,7 +15573,7 @@
 <H1>OPTION: ssh-path</H1>
 
 Sets the name of the command used to open a UNIX secure shell
-connection. The default is typically <CODE>/usr/local/bin/ssh</CODE>.
+connection. The default is typically <CODE>/usr/bin/ssh</CODE>.
 <P>
 <UL>   
 <LI><A HREF="h_finding_help">Finding more information and requesting help</A>
@@ -15928,7 +15928,7 @@
 <h1>OPTION: News-Spool-Directory</h1>
 This option tells Pine where to look for the "news spool" for newsgroups
 when accessing news locally, rather than via NNTP.  The default path is
-usually "/usr/spool/news".<p>
+usually "/var/spool/news".<p>
 <UL>   
 <LI><A HREF="h_finding_help">Finding more information and requesting help</A>
 </UL><P>
@@ -16303,16 +16303,16 @@
 <P>
 So, here are some example entries:
 <PRE>
-url-viewers = _TEST(&quot;test -n '$&#123;DISPLAY}'&quot;)_  /usr/local/bin/netscape
-              &quot;/usr/local/bin/lynx _URL_&quot;
+url-viewers = _TEST(&quot;test -n '$&#123;DISPLAY}'&quot;)_  /usr/bin/netscape
+              &quot;/usr/bin/lynx _URL_&quot;
               C:&#92;BIN&#92;NETSCAPE.BAT
 </PRE>
 <P>
 This example shows that for the first viewer in the list to be used
 the environment variable &quot;DISPLAY&quot; must be defined.  If it
-is, then the path and file &quot;/usr/local/bin/netscape&quot; must exist.  
+is, then the path and file &quot;/usr/bin/netscape&quot; must exist.  
 If neither condition is met,
-then the path and file &quot;/usr/local/bin/lynx&quot; must exist.  
+then the path and file &quot;/usr/bin/lynx&quot; must exist.  
 If it does, then the &quot;_URL_&quot; token is replaced by the selected URL. 
 If the path to &quot;lynx&quot; is invalid,
 then the final path and file C:&#92;BIN&#92;NETSCAPE.BAT must exist.  
@@ -16405,10 +16405,10 @@
 An example using the Korn shell and the Netscape browser (first entry wrapped 
 because of its length, but should all appear on one line):
 <P>
-url-viewers = _TEST("test -L /myhomedir/.netscape/lock")_ &quot;/usr/local/bin/netscape -remote 'openURL(_URL_,  new-window)' &amp;&quot;<BR>
+url-viewers = _TEST("test -L /myhomedir/.netscape/lock")_ &quot;/usr/bin/netscape -remote 'openURL(_URL_,  new-window)' &amp;&quot;<BR>
 
-_TEST(&quot;test -n '$&#123;DISPLAY}'&quot;)_ &quot;/usr/local/bin/netscape &amp;&quot;<BR>
-              &quot;/usr/local/bin/lynx '_URL_'&quot;
+_TEST(&quot;test -n '$&#123;DISPLAY}'&quot;)_ &quot;/usr/bin/netscape &amp;&quot;<BR>
+              &quot;/usr/bin/lynx '_URL_'&quot;
 <P>
 <!--chtml endif-->
 <P>

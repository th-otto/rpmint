diff -rup -x configure -x Makefile.in -x Makefile -x aclocal.m4 xgalaga.orig/configure.ac xgalaga.new/configure.ac
--- xgalaga.orig/configure.ac	2023-12-12 04:53:44.767741513 +0100
+++ xgalaga.new/configure.ac	2023-12-12 05:21:22.931702925 +0100
@@ -9,7 +9,6 @@ AM_PROG_CC_C_O
 # Enable compilation warnings if gcc is used.
 if test "${ac_cv_c_compiler_gnu}" = "yes"; then
   CFLAGS="$CFLAGS \
-    -ggdb \
     -Wall \
 	-W \
     -Wcast-align \
diff -rup -x configure -x Makefile.in -x Makefile -x aclocal.m4 xgalaga.orig/src/data.c xgalaga.new/src/data.c
--- xgalaga.orig/src/data.c	2023-12-12 04:53:44.777741495 +0100
+++ xgalaga.new/src/data.c	2023-12-12 05:25:43.834630483 +0100
@@ -89,4 +89,4 @@ SFont_Font *fnt_big_red;
 
 enum gstate gstate;
 
-int fullscreen = 1;				/* windowed = 0, fullscreen != 0 */
+int fullscreen = 0;				/* windowed = 0, fullscreen != 0 */
diff -rup -x configure -x Makefile.in -x Makefile -x aclocal.m4 xgalaga.orig/src/defs.h xgalaga.new/src/defs.h
--- xgalaga.orig/src/defs.h	2023-12-12 04:53:44.781074822 +0100
+++ xgalaga.new/src/defs.h	2023-12-12 05:49:44.882389403 +0100
@@ -114,4 +114,8 @@
 
 #define MAXFILENAME 1024
 
+#ifdef __MINT__
+#define SCORE_FILE_NAME "highscor.dat"
+#else
 #define SCORE_FILE_NAME ".xgalaga-sdl"
+#endif
diff -rup -x configure -x Makefile.in -x Makefile -x aclocal.m4 xgalaga.orig/src/highscore.c xgalaga.new/src/highscore.c
--- xgalaga.orig/src/highscore.c	2023-12-12 04:53:44.781074822 +0100
+++ xgalaga.new/src/highscore.c	2023-12-12 05:50:30.058985814 +0100
@@ -137,7 +137,7 @@ static void save_scores(void)
     long x;
     char my_file_name [256], *home;
 
-#ifdef __WII__
+#if defined(__WII__) || defined(__MINT__)
 	home = DATADIR;
 #else
 	home = getenv("HOME");
@@ -294,7 +294,7 @@ void load_scores(void)
     int hsf;
     char my_file_name[256], *home;
 
-#ifdef __WII__
+#if defined(__WII__) || defined(__MINT__)
 	home = DATADIR;
 #else
 	home = getenv("HOME");
diff -rup -x configure -x Makefile.in -x Makefile -x aclocal.m4 xgalaga.orig/src/main.c xgalaga.new/src/main.c
--- xgalaga.orig/src/main.c	2023-12-12 04:53:44.781074822 +0100
+++ xgalaga.new/src/main.c	2023-12-12 05:39:02.986720971 +0100
@@ -88,6 +88,7 @@ static void print_usage(void)
 		   "  -nosound             Turn sound OFF\n"
 		   "  -level <number>      Choose starting level (>= 1)\n"
 		   "  -window              Start in windowed mode instead of fullscreen\n"
+		   "  -fullscreen          Start in fullscreen mode instead of windowed\n"
 		   "  -winsize <WxH>       Window size (default 468 x 596)\n"
 		   "\n"
 		   "This game is free software, under the GPLv2\n"
@@ -876,6 +877,8 @@ static void do_player(int but)
                 break;
 
             case SDLK_SPACE:
+            case SDLK_LCTRL:
+            case SDLK_RCTRL:
                 keys &= ~FIREKEY;
 				break;
 
@@ -902,6 +905,8 @@ static void do_player(int but)
                 break;
 
             case SDLK_SPACE:
+            case SDLK_LCTRL:
+            case SDLK_RCTRL:
                 keys |= FIREKEY;
 				break;
 
@@ -1277,6 +1282,8 @@ int main(int argc, char *argv[])
                 wantStars = 0;
             } else if (strcmp(argv[ac], "-window") == 0) {
                 fullscreen = 0;
+            } else if (strcmp(argv[ac], "-fullscreen") == 0) {
+                fullscreen = 1;
             } else if ((strcmp(argv[ac], "-winsize") == 0) && (++ac < argc) &&
                        (sscanf(argv[ac], "%dx%d", &w, &h) == 2)) {
                 winwidth  = w;
